"""UI components split out of the monolithic Streamlit app.

Each module inside `ui` should focus purely on presentation / user interaction
logic, delegating side-effects and data manipulation to the `helpers` package or
other business-logic layers.
"""